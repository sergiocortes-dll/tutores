import { Box, Typography } from "@u_ui/u-ui";

export default function Create() {
  return (
    <Box sx={{ display: 'inline-flex', width: '100%', minHeight: 'calc(100dvh - 58px)', justifyContent: 'center', alignItems: 'center'}}>
      <Typography variant="h3" component="h2">Preparate...</Typography>
    </Box>
  )
}
